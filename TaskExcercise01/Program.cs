namespace TaskExcercise01
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //Create 4 tasks that invoke a method with name as an input parameter
            //Retrieve the task status for all of them.
            //Launch the tasks using differents ways to do it.
            //Retrieve the task status for all of them.
            //Use WaitAll for getting the messages.
            //Retrieve the task status for all of them
            

            Console.ReadLine();
        }

        private static void MethodToInvoke(object name)
        {
            //Send to the standard output a message indicating the beginning and ending of the method.
            //wait for 5 seconds between the messages.
        }
    }
}
