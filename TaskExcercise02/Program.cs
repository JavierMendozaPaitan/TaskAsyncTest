namespace TaskExcercise02
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //Create a list of tasks with a function that waits for a random period of time (miliseconds to seconds)
            //Launch all the tasks from the list
            //consider task data as status and taskid
            //print the initial task data
            //wait for any of them
            //print the task data 
            //Send a cancellation token to all of them
            //print the final task data
        }
    }
}
